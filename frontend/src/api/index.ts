/**
 * API Module Index
 * Re-exports all API functions from the main api.ts for backwards compatibility.
 * New code can import from specific modules in this directory.
 */

// Re-export everything from the original api.ts for full backwards compatibility
export * from '../api';
